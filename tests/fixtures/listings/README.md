# Listing regression fixture

`translation-placeholder.md` preserves the minimized shape of the confirmed adjacency-only acceptance defect: a correctly numbered caption and adjacent fenced block previously passed even when the block contained only an omission sentence. `translation-complete.md` is the control using the corresponding source code from `source-listing.txt`.

The fixture deliberately stays small so failures identify Listing gate behavior rather than a full paper translation.
